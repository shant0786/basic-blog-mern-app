import Layout from "../Layout/Layout.jsx";

const InvalidPage = () => {
    return (
        <Layout>
            <h1>This is Invalid Page</h1>
        </Layout>
    );
};

export default InvalidPage;